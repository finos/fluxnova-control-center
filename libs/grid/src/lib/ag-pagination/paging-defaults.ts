import { DEFAULT_RESULT_COUNT, MAX_RESULT_COUNT } from '@fxn/types';

export const defaultPage = 1;
export const defaultPageSize = DEFAULT_RESULT_COUNT;
export const pageSizeMin = DEFAULT_RESULT_COUNT;
export const pageSizeMax = MAX_RESULT_COUNT;
